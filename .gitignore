.DS_Store

# usdz is Apple-AR only — not used by the web page or for editing
cassette-tape/*.usdz
# NOTE: textures/ and source/ are tracked on purpose — they are the editable
# masters for retexturing this import template. See cassette-tape/MODEL.md
